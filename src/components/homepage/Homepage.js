import React from 'react';
import Header from '../header/Header';
import './Homepage.css'

const Homepage = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Homepage;